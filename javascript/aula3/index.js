const firstName = 'Leonardo'
const age = 27
const weight = 69
const height = 1.73
const imc = weight / (height ** 2)

console.log(`${firstName} is ${age} years old, weight: ${weight} kg, height: ${height} so IMC is ${imc}`)